// Top-level build file. Individual module (app) build files declare the
// Android Gradle Plugin / Kotlin plugin versions via the version catalog
// mechanism below — kept intentionally minimal for this M0 scaffold.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
