# Add project specific ProGuard rules here.
# renderengine_lite has no reflection-based JNI registration (JNIEXPORT
# functions are resolved by name at load time), so no keep rules are
# currently required for the native bridge.
